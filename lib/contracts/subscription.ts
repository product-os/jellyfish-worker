import type { ContractDefinition } from 'autumndb';

export const subscription: ContractDefinition = {
	slug: 'subscription',
	type: 'type@1.0.0',
	name: 'Subscription',
	markers: [],
	data: {
		schema: {
			type: 'object',
			properties: {},
		},
	},
};
